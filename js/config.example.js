// Copy this file to js/config.js and fill in a real token.
// js/config.js is gitignored — it is never committed, so the token never
// touches this repo's history or the hosted GitHub Pages site.
//
// Generate a token at:
// GitHub -> Settings -> Developer settings -> Personal access tokens ->
// Fine-grained tokens -> Generate new token
//   Resource owner: this account
//   Repository access: Only select repositories -> this repo
//   Permissions: Contents -> Read and write
const CONFIG = {
  GITHUB_OWNER: "Nelson299",
  GITHUB_REPO: "Job_List",
  GITHUB_BRANCH: "main",
  GITHUB_TOKEN: "paste-your-token-here",
};
